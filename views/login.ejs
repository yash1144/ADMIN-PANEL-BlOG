<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #000;
            --secondary-color: #2c3e50;
            --page-bg-color: #222;
            --text-color: #333;
            --card-bg-color: #ffffff;
            --input-border-color: #e0e0e0;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: white;
            /* Right side of the background */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            position: relative;
            /* Needed for pseudo-element positioning */
            z-index: 0;
        }

        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 50%;
            height: 100%;
            background-color: rgb(30, 30, 30);
            /* Left side of the background */
            z-index: -1;
        }

        .login-container {
            display: flex;
            width: 900px;
            max-width: 90vw;
            height: 600px;
            background-color: var(--card-bg-color);
            border-radius: 20px;
            box-shadow: 1px 60px 100px #000;
            overflow: hidden;
        }

        .login-image-panel {
            flex: 1;
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('https://images.unsplash.com/photo-1447752875215-b2761acb3c5d?q=80&w=2070&auto=format&fit=crop') no-repeat center center;
            background-size: cover;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 40px;
            color: white;
        }

        .login-image-panel h2 {
            font-size: 2.8rem;
            font-weight: 600;
            margin-bottom: 10px;
        }

        .login-image-panel p {
            font-size: 1.1rem;
        }

        .login-form-panel {
            flex: 1;
            padding: 60px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            background-color: var(--card-bg-color);
        }

        .login-form-panel h1 {
            font-size: 2rem;
            font-weight: 600;
            margin-bottom: 10px;
            color: var(--secondary-color);
        }

        .login-form-panel p {
            margin-bottom: 30px;
            color: #777;
        }

        .input-group {
            position: relative;
            margin-bottom: 25px;
        }

        .input-group input {
            width: 100%;
            padding: 15px;
            border: 1px solid var(--input-border-color);
            border-radius: 10px;
            font-size: 1rem;
            transition: border-color 0.3s;
        }

        .input-group input:focus {
            outline: none;
            border-color: var(--primary-color);
        }

        .btn-login {
            width: 100%;
            padding: 15px;
            border: none;
            border-radius: 10px;
            background-color: var(--primary-color);
            color: white;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .btn-login:hover {
            background-color: #333;
        }

        @media (max-width: 768px) {
            .login-container {
                flex-direction: column;
                width: 100%;
                height: auto;
                max-width: 100vw;
                border-radius: 0;
            }

            .login-image-panel {
                display: none;
                /* Hide image on small screens */
            }

            .login-form-panel {
                padding: 40px;
            }
        }
    </style>
</head>

<body>
    <div class="login-container">
        <div class="login-image-panel">
            <h2>Nature</h2>
            <p>Relaxing Your Mind From Madness</p>
        </div>
        <div class="login-form-panel">
            <h1>Login Here!</h1>
            <p>Please enter your credentials to proceed.</p>
            <form action="/login" method="POST">
                <div class="input-group">
                    <input type="email" id="email" name="email" placeholder="Email Address" required>
                </div>
                <div class="input-group">
                    <input type="password" id="password" name="password" placeholder="Password" required>
                </div>
                <button type="submit" class="btn-login">Login</button>
            </form>
        </div>
    </div>
</body>

</html>